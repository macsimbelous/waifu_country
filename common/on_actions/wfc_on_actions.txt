on_actions = {

	# on_startup = {
	# 	effect = {
	# 		ACW = {
	# 			set_variable = {
	# 				acw_core_bonus_to_pp = 0.05
	# 			}
	# 			add_dynamic_modifier = {
	# 				modifier = acw_core_states_to_pp
	# 			}
	# 		}
	# 	}
	# }
	# on_weekly_ACW = {
	# 	effect = {
	# 		#every_owned_state = {
	# 		#	limit = {
	# 		#		is_fully_controlled_by = WFL
	# 		#		compliance > 0.9
	# 		#		resistance < 0.05
	# 		#		NOT = {
	# 		#			is_core_of = WFL
	# 		#		}
	# 		#	}
	# 		#	for_each_loop = {
	# 		#		array = core_countries
	# 		#		value = temp_tag
	# 		#		remove_core_of = temp_var:temp_tag
	# 		#	}
	# 		#	add_core_of = WFL
	# 		#}
	# 		set_temp_variable = {
	# 			temp = core_states
	# 		}
	# 		multiply_temp_variable = {
	# 			temp = 0.01
	# 		}
	# 		set_variable = {
	# 			acw_core_bonus_to_pp = temp
	# 		}
	# 	}
	# }
	#ROOT is subject FROM is overlord
	on_subject_annexed = {
		effect = {
			if = {
				limit = {
					FROM = {
						OR = {
							original_tag = ACW
							original_tag = HEN
							original_tag = MAH
							original_tag = TRP
							original_tag = MEH
							original_tag = MMS
						}
					}
				}
				ROOT = {
					every_owned_state = {
						for_each_loop = {
							array = core_countries
							value = temp_tag
							remove_core_of = temp_var:temp_tag
						}
						add_core_of = FROM
					}
				}
			}
		}
	}
}
